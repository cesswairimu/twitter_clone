<% if @user.errors.any? %>
    This signup application contains <%= pluralize(@user.errors.count, "error") %>
<%= @user.errors.full_messages.each do |msg| %>
    <ul class="error">
        <li> <%= msg %></li>
    </ul>
<% end %>
   <% end %>
